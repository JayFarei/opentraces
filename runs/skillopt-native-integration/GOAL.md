Goal: Complete kb/plans/084-skillopt-paper-fidelity-and-online-harness.md, bringing src/opentraces/consumers/skill_opt to line-level fidelity with SkillOpt Algorithm 1 (arXiv 2605.23904) and adding the paper's online loop via otbox, through the plan's three phases tracked in the run log. Phase 1 (offline, no agent): a 3-way Dtrain/Dsel/Dtest split with a reported held-out Dtest score, an epoch-local feedback buffer separated from a run-global audit log, reflection minibatches of size Bm, a longitudinal slow/meta update gated to e>=2, an optimizer-side teacher-only meta-skill, and an autonomous LR schedule. Phase 2 (harness abstraction, CI-safe): a Harness protocol (collect_rollouts + score) with BucketHarness refactored out, plus a test-side OtboxReRolloutRunner/OtboxHarness that forks the c-installed-source checkpoint, injects the candidate skill as CLAUDE.md, runs run_simulated_session, and scores via the captured trace's outcome_reward, proven end to end deterministically in default CI with the synthetic echo/fake-claude binary as a new otbox journey. Phase 3 (opt-in real loop): a small held-out verifiable scenario suite and one budgeted real otbox-driven optimization run with the real claude or codex binary, committed as a gold artifact showing a skill improving on a held-out Dtest score under a live agent. The work is done when all three phases are landed and the optimizer structurally matches Algorithm 1 in offline mode and runs the live loop in online mode. Verified by `pytest tests/test_skill_opt.py -q` and `pytest tests/ -q` green (triaging only environment-bound perf/integration tests), including new tests that surface a reported Dtest score, the epoch-local buffer reset, Bm minibatching, the e>=2 longitudinal slow update, and the teacher-only meta-skill; by the new otbox online-loop journey passing in default CI via the synthetic binary (`make otbox-journeys`); and by one committed opt-in real otbox run whose surfaced transcript shows the live agent re-rolling under a candidate skill and the gate accepting on the re-rolled held-out reward. Cross-check arXiv 2605.23904 (Algorithm 1 and Appendix C.2) each iteration and record the section checked. Preserve the full suite green, the additive-only schema contract in packages/opentraces-schema/VERSION-POLICY.md, the consumers contract and the PR pipeline (trail blame pr), and keep security tools default-off; the deterministic default path must never require a network or a live agent, and src/ must never import tests/. Touch only src/opentraces/consumers/skill_opt/, src/opentraces/workflow_templates/skill-opt-v1/, src/opentraces/cli/workflow.py, tests/ (including tests/otbox/), and the run log; read Trail and outcome data but do not modify the bucket, Trail, or Context Tree substrate write paths, publish/huggingface, or web/, and do not promote otbox to a shipped end-user surface. Log each iteration to runs/skillopt-native-integration/log.md with the phase, the paper section cross-checked, the change, the test / otbox-journey / real-run evidence, and the next-step rationale. On block (for example the agent binary or auth being unavailable for the Phase 3 real run, or tmux missing for otbox), append a BLOCKED entry to that log with attempted paths, evidence gathered, the blocker, and the input that would unlock progress.