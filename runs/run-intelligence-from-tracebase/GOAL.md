/goal Build the deterministic run-intelligence layer extracted from tracebase into opentraces, per the spec at kb/plans/086-run-intelligence-from-tracebase.md and the brief at kb/br/67-tracebase-local-trace-inspection.md (read both first; the plan carries the vetted designs, adversarial tightenings, requirements R1-R7, exact thresholds, and per-unit scope). Work in the prepared worktree at /Users/jayfarei/src/tries/community-traces-run-intel on branch feat/run-intelligence-from-tracebase (venv installed). Done means the five outcomes below, which cover R1-R7 (R6=outcome 5, R7=outcome 4): (1) `trace map/get <id> --waste` emit the frozen opentraces.context_waste.v1 envelope exactly per R1+R2 (three patterns, exact thresholds/confidences, deterministic finding_ids, timestamp-missing limitation); (2) a new `trace compare <a> <b>` emits the frozen opentraces.trace_compare.v1 envelope of {a,b,delta} triples over Metrics (reuse compute_metrics), deterministic quality (assess_trace enable_judge=False, persona_scores.get()), and burst/error/security signals (both traces pinned to DEFAULT_BURST_GAP=35), degrading missing-map blocks to available:false and exiting 6 on an unresolved ref; (3) `trace get/map <id> --run-intel` emit {status,trace_id,signals,counts} with deterministic resteer/recovery/loop annotations (recovery only after an uncleared failure; resteer suppressed for authorizations via is_trigger), ADDITIVE-only with NO action_type widening and NO signal_refs persistence; (4) each detector derives from the bare TraceRecord but prefers capture_method=otel fidelity inputs when present (plan 078), recording fidelity:"record"|"otel"; (5) zero schema change — git diff touches nothing under packages/opentraces-schema/ and no SCHEMA_VERSION bump.

Verified by, with output in the transcript: FIRST, before any edit, `source .venv/bin/activate` and run `pytest tests/ -q` on worktree HEAD, recording baseline pass/fail counts. Then implement, and run the six new files `pytest tests/core/test_context_waste.py tests/core/test_trace_compare.py tests/core/test_run_intel.py tests/cli/test_context_waste_cli.py tests/cli/test_trace_compare_cli.py tests/cli/test_run_intel_cli.py -q` and report the status line; paste one real envelope each for --waste, --run-intel, and `trace compare`; show the get-vs-map surface-parity proof and one trace run twice producing byte-identical JSON; show ONE envelope with fidelity:"otel" from an OTel-captured fixture, OR append a BLOCKED entry to log.md explaining why no such fixture is sourceable (R7 must not pass silently unexercised); then `pytest tests/ -q` and require the final passed count >= the recorded baseline (objective regression gate).

While preserving: the additive-evolution contract in packages/opentraces-schema/VERSION-POLICY.md; the bursts surface and other trace/trail/ctx commands stay identical; all three envelopes follow the frozen opentraces.*.v1 rule (any field change bumps to v2).

Use only: src/opentraces/core/{context_waste.py,trace_compare.py,run_intel.py} (new), src/opentraces/cli/trace.py, the six new tests above, and the CLAUDE.md trace-surface bullet. Reuse read-only and do not edit: enrichment/metrics.py, quality/engine.py, core/bursts.py, core/intent.py, core/trace_map.py, core/context_tree/query.py. Do not touch the schema package, capture parsers, publish/huggingface, web/viewer, or marketing site; do not add a persisted scorecard field, a command beyond `trace compare`, or encryption at rest.

Log each attempt to runs/run-intelligence-from-tracebase/log.md with the diff summary, evidence observed, and next-step rationale. On block, append a BLOCKED entry with attempted paths, evidence, the blocker, and what would unlock it — stop and surface if a frozen-envelope shape needs a product decision the plan cannot resolve, if assess_trace/compute_metrics cannot run deterministically on a synthetic fixture, or if the R7 OTel-capture fixture cannot be sourced without live capture.
