/goal Land the opentraces 0.3.3 → 0.4 migration acceptance suite in otbox, test-and-fix, per the spec at kb/plans/085-migration-0.3.3-to-0.4-test.md and the handoff at runs/migration-0.3.3-to-0.4/HANDOFF.md (read both first). Done means: (1) otbox can restore a frozen real-0.3.3 world via a new `c-legacy-v033` checkpoint built from the actual `v0.3.3` CLI tag (artifact-preferred, synthetic-fallback per plan 072) and drive a 0.4 upgrade over it; (2) all twelve migration scenarios S1–S12 pass as journeys in the tiered gate and inventory coverage map, with S12 the gold end-to-end ship gate; (3) the migration-correctness bugs the suite catches are fixed in this run — at minimum the removed `TraceRecord.patch` field is reconstructed into `patches[]` (fallback: preserved under `metadata.legacy.patch`) so a 0.3.0 record migrated through `migrate_outdated_shards` and the bucket-adoption path loses zero devtime data; (4) the S2 field-loss audit output proves `patch` is the only dropped field; (5) VERSION-POLICY.md, the schema CHANGELOG, and a rationale/migration note agree with what shipped.

Verified by, with output landing in the transcript: `source .venv/bin/activate` then run the migration journeys (`make otbox-journeys` plus the targeted `migration-*` / `legacy-v033-*` journeys) and report the pass/fail summary lines; run `pytest tests/ -q` and report the final status line; paste the S2 added/dropped-field audit; and show one before/after record proving `patch` survived as `patches[]`.

While preserving: every existing otbox slice (`make otbox-slice`/`otbox-matrix`/`otbox-inventory`) and the full `pytest tests/` suite stay green; the additive-schema contract is not violated further (no new field removals on a MINOR bump); the suite runs in default CI with no network using the synthetic fallback; the original 0.3.3 on-disk data is never destructively mutated (additive / write-new-and-swap only).

Use only: tests/otbox/**, packages/opentraces-schema/**, src/opentraces/publish/huggingface/**, src/opentraces/core/bucket_store.py, bucket_remote.py, migrate_trace_ids.py, and the migration/version docs; plus a throwaway isolated venv at tag `v0.3.3` to build the fixture. Do not modify unrelated subsystems (capture parsers, web/viewer, marketing site) or invent a third bucket-adoption path beyond what S5's Open Decision allows.

Log each attempt to runs/migration-0.3.3-to-0.4/log.md with the diff, the evidence observed (journey/pytest output, audit deltas), and the next-step rationale. On block, append a BLOCKED entry to that log with attempted paths, evidence gathered, the blocker, and the input that would unlock progress — stop and surface if S2 reveals a second breaking field change, the real `v0.3.3` venv cannot be built here, or S5 needs a product decision the spec's Open Decisions cannot resolve.
