/goal Deliver a genuine full-release 0.3.3 → 0.4 upgrade UAT for opentraces: prove a real 0.3.3 user (CLI 0.3.3 / schema 0.3.0) can pip-upgrade to 0.4 (CLI 0.4.0 / schema 0.6.0) and successfully USE 0.4, not merely avoid crashing. Read first and treat as authoritative, do not re-derive: runs/migration-0.3.3-to-0.4/UPGRADE-UAT-AUDIT.md (the 69-case catalogue, 35 P0 / 29 P1 / 5 P2, with its phased implementation plan and biggest-gaps ranking), kb/plans/085-migration-0.3.3-to-0.4-test.md, and the newest entries in runs/migration-0.3.3-to-0.4/log.md (which record the completed S1-S12 migration-core suite and the confirmed P0 read-path bug).

Done means, in priority order: (1) the confirmed P0 bug is fixed, the live read path (cli/trace.py:1058 _read_trace_record_from_path plus the parallel direct model_validate_json sites at 1271/1309/1559/1775, and any equivalent loader in core/trace_index.py / search_projection / bucket_store) routes legacy 0.3.0 records through opentraces_schema.migrations.migrate_record before validation, so trace get/query/map/slice expose reconstructed patches[] + metadata.legacy.patch instead of silently dropping outcome.patch; (2) regression guards land, U-trace-2 (pytest over the frozen legacy_world_v033 fixture) and U-trace-1 (otbox journey on c-legacy-v033 asserting patches[] non-empty + metadata.legacy.patch present through the live CLI); (3) the catalogue's P0 cases are implemented to green in the audit's phase order (Phase 1 fixture-only pytests, Phase 2 enrich the c-legacy-v033 checkpoint family incl. a real-OTLP upgraded checkpoint + the pty_runner step, Phase 3 the journey bulk), P0 before P1 before P2; (4) the parts only a real two-venv pip-0.3.3-then-pip-0.4 walk can prove (setup upgrade idempotency, auth reuse, no-surprise-egress, two-store raw-bucket-vs-dataset separation) are either covered by a real-v033-venv test or explicitly documented as manual-UAT with runnable steps.

Verified by, with output in the transcript: source .venv/bin/activate then run the migration + new upgrade journeys (make otbox-journeys plus the targeted migration-* / U-* journeys) and report pass/fail; pytest tests/test_migration_0_3_3_to_0_4.py tests/otbox/test_otbox_slice.py -q and report the final line; show one before/after proving trace get on a legacy id now returns non-empty patches[] (the P0 regression); and re-run otbox matrix --inventory --strict (expect drift OK) after adding journeys.

Constraints and scope: this phase EXPANDS the boundary beyond plan-085, src/opentraces/cli/trace.py and the read/index/bucket loaders ARE in scope for the P0 fix (justify any other src/ edit in the log). Keep the full pytest tests/ and every otbox slice green; the pre-existing trace-trails corpus-currency drift and tests/perf timing flakes are NOT your regressions, triage them separately. Do not violate the additive-schema contract (no new field removals). Do not boil the ocean, land P0s first in coherent commits, each phase verified before the next, defer P1/P2 if time-bound. Commit and push straight to main (repo preference). No long dashes in prose, use commas.

Log every iteration to runs/migration-0.3.3-to-0.4/log.md with the diff, the evidence observed, and the next-step rationale. On block, append a BLOCKED entry and stop and surface if: the read-path migration fix breaks a documented contract or a green test, a SECOND breaking schema field surfaces, the live HF publish path genuinely cannot reach migrate_record (a product decision), or a real claude/OTLP capture cannot run here (the real-v033 venv at /tmp/ot-v033-worktree/.venv-v033 may be absent and is rebuildable per the HANDOFF).
