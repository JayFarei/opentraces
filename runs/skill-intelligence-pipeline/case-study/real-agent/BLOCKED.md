# BLOCKED: Real-Agent Case Study

Attempted path: default Skill Intelligence dry-run.

Evidence gathered: the deterministic CI path completed without network, auth, or
live-agent requirements. No real-agent run was started because this case-study
leg must remain explicit opt-in.

Blocker: live Claude/Codex execution is intentionally outside the default CI
path.

Input that would unlock progress: explicitly run the real-agent leg with the
chosen binary/auth and opt-in environment for live execution.
