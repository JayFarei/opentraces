# skill-opt-v1 workflow tests

The builder and the optimizer loop are exercised end-to-end by
`tests/test_skill_opt.py` (bucket-seeded `build_rows.py` projection, and the
`opentraces workflow optimize --dry-run` artifact contract).
