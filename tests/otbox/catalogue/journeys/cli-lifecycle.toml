name = "cli-lifecycle"
description = """
Core project lifecycle: inspect a seeded project's status and config,
resolve a trace, then remove the project cleanly. Proves the
init -> inspect -> remove arc a normal user relies on.

Also exercises `config get` (issue #160's single-setting reader) so the
whole-dump `config show` and the key-addressed `config get` stay covered
side by side.
"""
lane = "core"
tier = 0
seed = "smoke"
requires = ["cli"]

[[steps]]
type = "cli"
id = "status"
argv = ["--json", "status"]

[[steps]]
type = "cli"
id = "config-show"
argv = ["config", "show"]

[[steps]]
type = "cli"
id = "config-get"
argv = ["--json", "config", "get", "classifier_sensitivity"]

[[steps]]
type = "cli"
id = "trace-get"
argv = ["trace", "get", "otbox000000000000"]

[[steps]]
type = "cli"
id = "remove"
argv = ["--json", "remove"]

[[assertions]]
kind = "returncode"
step = "status"
equals = 0

[[assertions]]
kind = "returncode"
step = "config-get"
equals = 0

[[assertions]]
kind = "stdout_json"
step = "config-get"
path = "key"
equals = "classifier_sensitivity"

[[assertions]]
kind = "stdout_contains"
step = "trace-get"
value = "otbox000000000000"

[[assertions]]
kind = "stdout_json"
step = "remove"
path = "next_command"
equals = "opentraces init"
