name = "trace-query-smoke"
domain = "cli"
lane = "smoke"
adapter = "subprocess"
target = "cli.trace_query"
fixture = "stack"
tier = "small"
repetitions = 5
warmups = 1

[params]
lex = "trace"
limit = 20
