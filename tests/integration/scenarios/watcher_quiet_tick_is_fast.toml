name = "watcher_quiet_tick_is_fast"
description = "When nothing has changed, a watcher tick returns without running backfill and stays under the local latency budget."

[[steps]]
type = "reset"

[[steps]]
type = "init_opentraces"

[[steps]]
type = "write_file"
path = "a.py"
content = "print('a1')\n"

[[steps]]
type = "commit"
message = "add a"

# Warm-up tick — populates cache + stamps last_watcher_run_at.
[[steps]]
type = "watcher_run_once"

# Second tick — no new commits, no new JSONL activity. It should remain
# comfortably below interactive latency, while allowing local Git/filesystem
# jitter in the broader suite.
[[steps]]
type = "watcher_run_once"

[[assertions]]
kind = "tick_field"
name = "backfill_invoked"
equals = false

[[assertions]]
kind = "tick_field"
name = "new_commits"
equals = 0

[[assertions]]
kind = "tick_field"
name = "duration_ms"
max = 250
